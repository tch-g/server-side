export interface SignIn {
    email: string;
    password: string;
}

export interface SignUp {
    firstName: string;
    lastName: string;
    nickName: string;
    email: string;
    password: string;
}